# HTTP/Socket.IO server port. Defaults to 8080 if not set.
PORT=8080

# Comma-separated list of allowed CORS/Socket.IO origins.
# Defaults to http://localhost:5173 if not set.
CORS_ORIGIN=http://localhost:5173
